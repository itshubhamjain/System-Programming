MACRO
	EVAL  &X,&Y,&Z
	MOVER  	AREG, &X
	SUB 	AREG, &Y
	ADD  	AREG, &Z
	MOVER  	AREG, &Z
MEND
MACRO
	CALC  &X,&Y,&OP=MULT,&LAB=
	MOVER  	AREG, &X
	&OP 	AREG, &Y
	MOVEM  	AREG, &LAB
MEND
START 
	MOVEM AREG, B
	EVAL A,B,C
	MOVEM AREG, N
	MOVEM AREG, N
	ADD AREG, N
	CALC P,Q,OP=DIV,LAB=NEXT
	M	DS	1
	A 	DS	5
END
